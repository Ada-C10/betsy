<div class="container">
  <h2 class="display-4 text-center">404</h2>
  <div class="row">
    <div class="col-sm-6">
      <%= image_tag("lost1.jpg", :alt => "404") %>
    </div>
    <div class="col-sm-6">
      <br />
      <p><%= "Uh oh! That page just went on vacation. Sorry about that." %></p>
      <br />
      <p><%= "Search No Regretsy" %></p>
      <%= form_tag search_path, method: :get, class: "form-inline" do %>
        <div class="md-form my-0">
          <%= text_field_tag(:search, params[:search], class: "form-control mr-sm-2") %>
          <%= submit_tag(:Search, class: "btn btn-primary") %>
        </div>
      <% end %>
    </div>
  </div>
</div>
